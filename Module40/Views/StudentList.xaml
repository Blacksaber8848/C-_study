<UserControl x:Class="Module40.Views.StudentList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid x:Name="LayoutRoot" Background="White" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
           
        </Grid.RowDefinitions>

        <ListBox x:Name="_listOfPeople" ItemsSource="{Binding People}"  Grid.Column="0"  Grid.Row="0"  DisplayMemberPath="Name" />
   
        <ContentControl Grid.Column="1" Grid.Row="0"
                 prism:RegionManager.RegionName="PersonDetailsRegion2"
                 prism:RegionManager.RegionContext="{Binding SelectedItem, ElementName=_listOfPeople}"/>
        <Button Content="Update Information"
			Margin="5"
			Command="{Binding UpdateButton_Click}"
               Grid.Row="1"  Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>