<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp"
        Title="MainWindow" Height="200" Width="300">
    <Window.Resources>
        <local:NumberToColorConverter x:Key="NumberToColorConverter"/>
    </Window.Resources>
    <StackPanel>
        <TextBox x:Name="numberTextBox"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Center"
                 Text="50"
            Background="{Binding Path=Text, RelativeSource={RelativeSource Self}, Converter={StaticResource NumberToColorConverter}, ConverterParameter=True, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">



        </TextBox>
    </StackPanel>
</Window>