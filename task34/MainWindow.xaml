<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="Window2" Height="450" Width="800">
    <Window.Resources>
        <Storyboard x:Key="sb" RepeatBehavior="Forever">
            <ColorAnimationUsingKeyFrames Storyboard.TargetName="light" Storyboard.TargetProperty="Background.(Color)">
                <DiscreteColorKeyFrame Value="White" KeyTime="0:0:0.2"/>
                <DiscreteColorKeyFrame Value="Red" KeyTime="0:0:0.4"/>
            </ColorAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <Button x:Name="button" Width="100" Height="30" Margin="0 150 0 0"  Click="button_Click" Content="click me!"/>
        <Border x:Name="light" Height="60" Width="60" CornerRadius="30" Background="Red" RenderTransformOrigin="0.5,0.5"/>
    </Grid>
</Window>