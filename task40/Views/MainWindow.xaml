<Window x:Class="task40.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
         xmlns:ns1="clr-namespace:Module40.Views;assembly=Module40"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525"
       >
    <Grid>
      
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>     

        <!--<ns1:StudentList Grid.Row="0"/>-->
        <ContentControl prism:RegionManager.RegionName="ContentRegion"  Grid.Row="0"/>
        <ContentControl prism:RegionManager.RegionName="ContentRegion2"  Grid.Row="1"/>
   
        <!--<ns1:StudentList Grid.Row="1"/>-->
    </Grid>
</Window>
